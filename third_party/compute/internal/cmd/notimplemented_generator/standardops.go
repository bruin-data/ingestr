// Copyright 2023-2026 The GoMLX Authors. SPDX-License-Identifier: Apache-2.0

package main

import (
	"os"
	"os/exec"
	"path"
	"slices"
	"strings"
	"text/template"

	"fmt"

	"github.com/gomlx/compute/internal/must"
	"github.com/gomlx/compute/support/backendparser"
	"github.com/gomlx/compute/support/sets"
	"k8s.io/klog/v2"
)

const (
	standardOpsInterfaceFile = "gen_standard_ops.go"
)

var (
	// methodsNotGenerated because they are maintained manually.
	methodsNotGenerated = sets.MakeWith(
		"Constant", "Parameter", "Identity", "ReduceWindow",
		"BatchNormForInference", "BatchNormForTraining", "BatchNormGradient",
		"And", "Or", "Xor", "Not", "ReduceAnd", "ReduceOr", "ReduceXor", "ScatterAdd",
		"AllReduce",                   // Output is not standard
		"FusedAttentionQKVProjection", // Multi-output op, maintained manually
		"Return", "Closure", "Name",
	)

	// methodsExcluded from generating and even from having a NodeType.
	// These are utility methods, not part of building a graph.
	methodsExcluded = sets.MakeWith(
		"Name", "Compile", "OpShape", "Main", "NewFunction",
		"DistributedSPMD", "DistributedAutoSharding", "DeviceAssignment")

	standardOpsTemplate = template.Must(template.New(standardOpsInterfaceFile).Parse(
		`/***** File generated by ./internal/cmd/notimplemented_generator, based on github.com/gomlx/compute/. Don't edit it directly. *****/

package notimplemented

import (
	"github.com/gomlx/compute"
	"github.com/gomlx/compute/shapes"
	"github.com/gomlx/compute/dtypes"
)

{{- range .}}
{{- range .Comments}}
{{.}}
{{- end}}
func (f Function) {{.Name}}({{range .Parameters}}{{.Name}} {{.Type}},{{end}}) (compute.Value, error) {
	return nil, f.baseErrFn(compute.OpType{{.Name}})
}
{{end}}
`))
)

// GenerateStandardOpsInterface generates the interface for the various standard ops.
// The rest of the ops are maintained manually.
func GenerateStandardOpsInterface(methods []backendparser.Method) {
	newMethods := make([]backendparser.Method, 0, len(methods))
	for _, method := range methods {
		if methodsExcluded.Has(method.Name) || methodsNotGenerated.Has(method.Name) {
			continue
		}
		if len(method.Outputs) != 2 || method.Outputs[0].Type != "Value" || method.Outputs[1].Type != "error" {
			// Non-conventional op, skipping.
			continue
		}
		for i := range method.Parameters {
			pi := &method.Parameters[i]
			switch pi.Type {
			case "Value":
				pi.Type = "compute.Value"
			case "...Value":
				pi.Type = "...compute.Value"
			case "[]Value":
				pi.Type = "[]compute.Value"
			case "Shape":
				pi.Type = "shapes.Shape"
			case "xla_data.FftType", "FFTType":
				pi.Type = "compute.FFTType"
			case "ConvolveAxesConfig":
				pi.Type = "compute.ConvolveAxesConfig"
			case "...PadAxis":
				pi.Type = "...compute.PadAxis"
			case "...DynamicPadAxis":
				pi.Type = "...compute.DynamicPadAxis"
			case "...DynamicDimensionSpec":
				pi.Type = "...compute.DynamicDimensionSpec"
			case "ActivationType":
				pi.Type = "compute.ActivationType"
			case "DenseConfig":
				pi.Type = "compute.DenseConfig"
			case "DenseLayout":
				pi.Type = "compute.DenseLayout"
			case "AttentionAxesLayout":
				pi.Type = "compute.AttentionAxesLayout"
			case "AxesLayout":
				pi.Type = "compute.AttentionAxesLayout"
			case "QuantizationScheme":
				pi.Type = "compute.QuantizationScheme"
			case "DotGeneralConfig":
				pi.Type = "compute.DotGeneralConfig"
			case "CumSumOptions":
				pi.Type = "compute.CumSumOptions"
			case "*Quantization":
				pi.Type = "*compute.Quantization"
			case "*ScaledDotProductAttentionConfig":
				pi.Type = "*compute.ScaledDotProductAttentionConfig"
			}
		}
		newMethods = append(newMethods, method)
	}
	methods = newMethods
	slices.SortFunc(methods, func(a, b backendparser.Method) int { return strings.Compare(a.Name, b.Name) })

	fileName := path.Join(must.M1(os.Getwd()), standardOpsInterfaceFile)
	f := must.M1(os.Create(fileName))
	must.M(standardOpsTemplate.Execute(f, methods))
	cmd := exec.Command("go", "fmt", fileName)
	klog.V(1).Infof("\t%s\n", cmd)
	must.M(cmd.Run())
	fmt.Printf("✅ notimplemented_generator:\tsuccessfully generated %s\n", fileName)
}
